import React, { Component } from 'react';

/** A component for the BalanceSheet/Volitility interface in the Dashboard.
 * AsOf(03/14/21), by Steven: alot of ambuguity surrounding this component, its behaviors, and what not. Needs more discussion.
 */
class BalanceSheetVolPanel extends Component {
    
    constructor(props) {
        super(props);

        // State stuff
    }

    render() {
        return(<h1>InDev</h1>)
    }
};

export default BalanceSheetVolPanel;